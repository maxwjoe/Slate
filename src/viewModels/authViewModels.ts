// loginViewModel : View model for login page
export interface loginViewModel {
  email: string;
  password: string;
}

// registerViewModel : View model for register page
export interface registerViewModel {
  username: string;
  email: string;
  password: string;
  confirmPassword: string;
}
